# PliegoCSS 0.1.0-rc.1

> Historical release record. It has no current promotion authority. See the
> schema-3 [machine readiness record](../product/release-readiness-0.1.0.json).

The first PliegoCSS release candidate established the standards-first CSS
compiler, verifier, and evidence pipeline written in Rust. It supported
ordinary CSS adoption alongside an optional compile-time typed Rust frontend.

## Historical highlights

- Audit ordinary CSS with human, canonical JSON, and SARIF findings.
- Transform and minify standard CSS with explicit compatibility targets.
- Compile typed `pc!` and `pcx!` Rust literals to deterministic static CSS.
- Resolve TOML themes and DTCG 2025.10 token inputs.
- Publish manifests, Source Map v3, token graphs, findings, and receipts as
  controlled groups.
- Enforce bounded compatibility, accessibility, ownership, budget, and usage
  policies.
- Inventory Sass, Tailwind CSS v4, CSS Modules, templates, configs, and plugin
  seams without executing their original toolchains.
- Prepare exact, reversible Tailwind projections with hash-bound apply and
  rollback.
- Provide a bounded stdio LSP and candidate VS Code/Neovim clients.
- Package nineteen exact-version crates with Rust 1.85 as the library MSRV.

## Historical distribution

The immutable GitHub prerelease remains available at
[`v0.1.0-rc.1`](https://github.com/celiumsai/pliegocss/releases/tag/v0.1.0-rc.1).
It predates the corrected public-preview source and was not published to
crates.io. Its tag must not be moved.

## Superseded by rc.2

`0.1.0-rc.2` supersedes this candidate with the complete brand system,
bilingual website, corrected audit findings, current evidence, and the first
authorized crates.io compatibility unit.

See the [rc.2 release notes](./0.1.0-rc.2.md), the
[README](../../README.md), and the
[release readiness document](../product/release-readiness-0.1.0.md).

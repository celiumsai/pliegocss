# Historical documents

Everything below `docs/archive/` is retained for provenance only. Archived files never determine
the current release state, satisfy a current coverage requirement, or authorize publication.

The sole machine authority for current release status is
[`../product/release-readiness-0.1.0.json`](../product/release-readiness-0.1.0.json). Its generated
Markdown projection is the only human-readable current-status page.

Historical evidence may be cited by a current check only when schema 3 records its exact source
commit, Git tree, observation window, environment, coverage, and immutable artifact hash.

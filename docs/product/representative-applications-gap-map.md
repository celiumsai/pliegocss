# Representative applications and selected gaps

Status: **five owned application gates complete; classification coverage is selected next**

The machine authority is
[`representative-applications-gap-map.json`](./representative-applications-gap-map.json). Owned
applications establish bounded product behavior and expose the next engineering gaps.

| Application | Proven boundary | Remaining gap |
|---|---|---|
| Plain HTML audit-first | Ordinary CSS adoption, budgets, static accessibility, audit receipt | Complete CSS classification |
| Vite/Tailwind inventory-first | Tailwind source/template inventory, v3/v4 coexistence, exact rollback, browser equivalence | Dynamic templates/config/plugins |
| CSS Modules consumer | Source, consumer-use, and composition inventory | Export/bundler proof and reversible CSS Modules migration |
| Typed Rust controlled build | Rust 1.85 API and controlled artifacts | Registry replay and clean external consumption |
| Framework-neutral routes | Asset Plan, Project Index, physical trace, controlled bundle group | Configured PliegoRS SSG/browser replay and published adapter compatibility |

## Priority order

1. **Classification corpus** — release blocking for broader standard-CSS claims. Expand the closed
   declaration, value, selector, and at-rule corpus without overstating unsupported coverage.
2. **Published RC replay** — release blocking. A repository checkout does not activate installation
   or SemVer promises.
3. **Dynamic Tailwind seams** — intentionally outside schema 1 and not part of the 0.1.x claim.
4. **CSS Modules reversible migration** — useful but outside the promoted G6 slice.

## Development order

The selected next gap is `classification-corpus`. Real-world usage and feedback may reprioritize
later work as they arrive naturally, but no interview, incident, pilot, project, or organization
quota blocks current engineering.

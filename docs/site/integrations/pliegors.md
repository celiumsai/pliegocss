<!-- pliegocss-site
{"schemaVersion":1,"route":"/docs/integrations/pliegors/","category":"Integrations","eyebrow":"PliegoRS integration","order":6}
-->

# Native together. Independent by contract.

PliegoRS is the first native consumer, not the only target. It supplies topology; PliegoCSS supplies style identity and artifacts.

## Explicit ownership {#boundary}

PliegoRS owns routes, pages, assets, and browser lifecycle. PliegoCSS owns parsing, semantic conflicts, CSS, manifests, and policy findings.

## Route and island bundles {#bundles}

A reachability sidecar projects application-owned topology into selected CSS bundles without PliegoCSS pretending to understand the framework.

## Replayed from published crates {#proof}

The maintained fixture locks PliegoRS 0.0.2 registry packages and verifies the remote-reachable framework revision separately.

```console
PLIEGORS_ROOT=/path/to/pliegors pnpm integration:pliegors
```

# Reachability-pruning benchmark

Date: 2026-07-22

Status: deterministic change gate for the opt-in application-union pruning contract

This benchmark isolates what `--prune-unreachable` does when a trusted reachability-schema-1
sidecar marks only part of a scanned source snapshot as reachable. It is a deliberately controlled
payload, not a representative application sample and not evidence of a universal percentage
reduction.

## Fixture and method

The harness creates a temporary, byte-deterministic Rust source and sidecar. The source contains 14
distinct `pc!` sites:

- one component referenced by route `home`;
- one component referenced by island `counter`; and
- twelve declared components referenced by no route or island.

The twelve dead sites use real multi-utility styles from the existing fixture vocabulary. Their
large share of this synthetic input makes the percentage reduction intentionally unsuitable for a
product-size claim.

The harness runs eight schema-5 compilations against the same generated bytes:

1. baseline A without pruning;
2. baseline B without pruning;
3. pruned A with `--prune-unreachable`; and
4. pruned B with `--prune-unreachable`.

It repeats those four profiles with `--theme`. The themed pair proves that normal emission retains
the complete supported seed variable block while pruning emits only variable-backed tokens directly
referenced by retained semantic styles.

Each repeated pair must produce byte-identical CSS and manifest files. Both manifests must retain
valid CSS hashes and byte counts and a closed graph-schema-2 projection. The gate checks canonical
node/edge order, global node identity, exact source ranges, non-dangling endpoints, component
ownership for every semantic declaration, physical contribution for every semantic declaration,
one rule owner for every physical declaration, and a semantic or synthetic cause for every
physical declaration.

## Frozen result

The reviewed source SHA-256 is
`159233dbb9258f93bbc0561e795780aa3b803a2cab8bfa3b19d37284d40359c0`; the generated sidecar
SHA-256 is `d457dd60004d9d989d42928c5665a5ed84561bf4955f61af3a23ecf21b876a76`.

The 2026-07-22 refresh keeps those inputs and every graph count unchanged while accepting the
StyleId-format-2 output identities from the shared compiler engine. Rust 1.85/1.96 CI lanes on
Linux and macOS produced one identical result, and isolated Node 22.13/24.16 replays reproduced the
same CSS, manifest, size, and gzip hashes before this snapshot was updated.

| Profile | Styles | Semantic declarations | Token nodes | Physical rules | Physical declarations | Raw | Gzip -9 |
|---|---:|---:|---:|---:|---:|---:|---:|
| Baseline schema 5 | 14 | 147 | 30 | 60 | 207 | 7,282 B | 1,213 B |
| Pruned schema 5 | 2 | 13 | 9 | 2 | 15 | 366 B | 224 B |
| Difference | -12 | -134 | -21 | -58 | -192 | -6,916 B (94.974%) | -989 B (81.533%) |

| Themed profile | Theme custom properties | Physical rules | Physical declarations | Raw | Gzip -9 |
|---|---:|---:|---:|---:|---:|
| Baseline schema 5 | 10 | 61 | 217 | 7,653 B | 1,369 B |
| Pruned schema 5 | 3 | 3 | 18 | 457 B | 265 B |
| Difference | -7 | -58 | -199 | -7,196 B (94.028%) | -1,104 B (80.643%) |

The retained variables are exactly `--color-accent`, `--color-ink`, and `--color-surface`; the
seven unused seed color/font variables are absent. The themed baseline CSS SHA-256 is
`cd2a276e221155f29cfc12ff3dbd8394cd144e314ccc1a753ad097b1178cb70f`; the themed pruned hash is
`a3b4b91dad2f3e627f56d219cf476bd1dfb22b12c5669c8f26af469d380a7799`.

The baseline CSS SHA-256 is
`a0d4a94b0be27ecbd113b764eca1d20ccbf5c328c896b3f2ca1a0f86516859ee`; the pruned CSS SHA-256
is `b1e26159f1078fd29403b5f70fb85fed7d1e290941dc07c91a476d96b317222a`. Their manifest hashes
are `a9e293c0f64cfff3658f0ca8d041c9ce659427915bb0064401999bf7ff210b5a` and
`8a991deb50fbb5299e3de3008297570bd14c62af2b47f47fe9449e344b7d241b`, respectively.

Gzip byte counts are frozen across the reviewed runtimes. The harness normalizes the informational
OS byte in the gzip header to `255` before hashing, then records and checks the deflate result for
each reviewed zlib build:

| Runtime zlib | Baseline gzip SHA-256 | Pruned gzip SHA-256 | Themed baseline | Themed pruned |
|---|---|---|---|---|
| Node 22.13, `1.3.0.1-motley-82a5fec` | `941b57383fd67450ae34234876b2b09d64dd6fb68dd138503858b9c4dfa5b4b1` | `4b62e9053298bcfb12e07330bac4c47c68a7c45815832d4d087f89e754a4501c` | `a13dee3f2d2f5093c7b3564d17b6aad9869a05618fb887bd2c0b359dc4a57814` | `8670a47312abec7e2fe58770c2a9c136aa7529eac104923acc9c6efea4f0312c` |
| Node 24.16, `1.3.1-e00f703` | `941b57383fd67450ae34234876b2b09d64dd6fb68dd138503858b9c4dfa5b4b1` | `4b62e9053298bcfb12e07330bac4c47c68a7c45815832d4d087f89e754a4501c` | `a13dee3f2d2f5093c7b3564d17b6aad9869a05618fb887bd2c0b359dc4a57814` | `8670a47312abec7e2fe58770c2a9c136aa7529eac104923acc9c6efea4f0312c` |

The gate fails if any frozen input, CSS, manifest, reviewed gzip hash, size, or graph count changes.
It also fails independently if raw or gzip output stops getting smaller, even if someone updates a
reviewed hash.

## Scope boundary

The benchmark proves whole-style removal and direct theme-variable filtering under the current union
of declared route and island roots. It does not measure route-specific loading, shared extraction,
critical CSS, runtime latency, or a complete PliegoRS application. It also does not discover
arbitrary authored CSS `var(...)` consumers: projects relying on those references must leave pruning
disabled or ensure each required variable has a reachable semantic consumer.

## Running the gate

Use the exact reviewed check:

```console
pnpm check:pruning
```

Run the harness without `--check` to print a current machine report without weakening the frozen
contract:

```console
node scripts/measure-reachability-pruning.mjs
```

The script requires Node 22.13 or newer. It uses `PLIEGO_CSSC` when provided; otherwise it uses the
portable `target/debug/pliego-cssc` executable and builds that target with Rust 1.96 only when it is
missing. CI supplies its already-built matrix executable. Temporary inputs and artifacts live only
under `target/benchmarks/reachability-pruning/run-<pid>`; the harness validates that containment
before deleting the directory on exit.

The executable harness is
[`scripts/measure-reachability-pruning.mjs`](../../scripts/measure-reachability-pruning.mjs).

# pliego-css-publication

Rollback-capable filesystem publication primitives shared by PliegoCSS tools. Transactions can sync
file and directory metadata when requested, but intentionally do not claim crash atomicity.
